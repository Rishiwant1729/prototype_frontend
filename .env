# For sharing the repo, use `frontend/.env.example`.
# Must include protocol and `/api` because the frontend calls endpoints like `/login`.
VITE_API_BASE_URL=https://prototypebackend-production-9fbb.up.railway.app/api
